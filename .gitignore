# Build output
/target
/src-tauri/target
/dist
/web/dist
node_modules/
*.tsbuildinfo

# Tauri's generated schemas and bindings. Regenerated by every build; the
# capability files that reference them are checked in, the output is not.
/src-tauri/gen

# Window-gate evidence. A screenshot lives on the issue, not in the clone
# (docs/rules/done.md), so the folder the driver writes into is local only.
/evidence

# Code graph (generated by Demido Studio)
graphify-out/

# Local state
.env
.env.local
*.log

# OS
Thumbs.db
.DS_Store

# Wayfinder research scratch
/.research
