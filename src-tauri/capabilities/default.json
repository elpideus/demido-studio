{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the two windows are allowed to call: the desk, and the splash that opens ahead of it. Nothing is added here for the window gate: the driving relaxation is a merged dev config and never a permission (docs/rules/done.md).",
  "windows": ["main", "splash"],
  "permissions": ["core:default"]
}
