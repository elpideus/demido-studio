Port skill at `$source` into Demido Studio format, install it.

Conversion notes from user (may be empty): $notes

**Read the Skill Manager guide first.** Its `SKILL.md` path is in your context (skills block). This body is sent alone — guide is NOT in your context. Do not proceed from memory. Porting section is at the end of that file.

Source is spec for **content**, not wording: every instruction survives, prose compresses to Caveman Ultra.

**Before anything else:** this task deletes nothing, modifies nothing. `read_file` + `list_dir` on source, then `install_skill` for result — only tools it needs. Do **not** call `delete_skill`; in particular do not delete `skill-manager`, which is the skill running right now, not the one being converted. Source at `$source` is read-only: never write to it. First tool call = `list_dir`.

1. **Read source.** `list_dir` the path → `read_file` every text file in it. `$source` is single file → read it. Reads fail because no working folder set → stop, say so, ask user to set one or paste skill text directly. **Do not skip this. Do not install anything you have not read.** Cannot convert a skill whose text you never saw; inventing plausible content = failed port that looks finished.
2. **Classify by structure**, using recognition table: frontmatter shape, `commands/` vs `workflows/` vs `rules/`, bare doc, `mcpServers` config. Do not infer from folder name. **Source with no `commands/` folder is not commandless** — in Claude Code the skill itself is `/<name> [args]` → materialise that command explicitly, reading body's own prose (e.g. a `/name a|b|c` line) for arg spec. Demido creates no command implicitly.
3. **Split content.** Per chunk ask: always true, or run once on request? Always-true → `SKILL.md`. Run-once user types → `commands/<name>.md` body. Model-invoked → `tools.json` prompt entry. Strip frontmatter. Every declared command needs real body — a `file` you also ship, or non-empty inline `prompt`. Install rejects neither.
4. **Compress prose to Caveman Ultra.** Fragments, one idea per line, no articles/filler/hedging, arrows for causality. **Every fact, rule, step, exception, example survives.** Code blocks, JSON, tool names, paths, CLI commands, error strings, placeholder syntax, `description` trigger words: verbatim. Passage that compression makes ambiguous → keep long, flag in report. Compressing is translation, not editing — never drop a step you think redundant, never add one.
5. **Translate plumbing.** Metadata → `skill.json` (`id` = kebab-case folder name, keep source `description` trigger wording — sole trigger in Demido). Argument hints → `params` schema, `required` for what command can't run without, `rest` last for trailing free text. Foreign placeholder dialects → `\$ARGUMENTS` / `\$1` / `\$name`. Foreign tool names → Demido builtins. Shell → PowerShell. Escape any placeholder a body merely discusses. Any ported body that assumed skill text was in context → add line telling it to `read_file` the `SKILL.md`; in Demido it never is.
6. **Source MCP config** (`mcp.json`, `.mcp.json`, `mcpServers` key) → `tools.json` entry `{"type":"mcp"}`, same `command` / `args` / `env`. Warn user: install prompts for it, showing that command line. Set `bypassAgentMode` only if source's tools clearly must run ungated — default off.
7. **Install** with `install_skill` — id + every file at relative path. Id = source folder name, kebab-cased; never `skill-manager`. Not `write_file`. Never `delete_skill` first — installing already replaces. *Different* skill with that id already installed → say so, confirm before replacing. `install_skill` not in tool list → stop, say so plainly, do not improvise.
8. **Report**, this order: new id + each command with usage line + each tool with its trigger; what was rewritten (tools, shell, placeholders, prose compressed); what was **dropped** and why (per-skill tool limits, model pins, hooks, pre-execution syntax); anything left uncompressed because compression would blur it; what used to auto-trigger and now needs toggle, `/` command, or model calling a tool. Then tell user to enable in Tools → Skills.

Ask before dropping anything notes did not authorise. Passage with no clean target → keep it, flag it. Lost instruction is worse than awkward one.
