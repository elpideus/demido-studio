# The brief, row by row

Every requirement in [`docs/brief.md`](brief.md), what has been decided about
it, what has been built, and whether a real model has ever driven it.

Checked by `scripts/check-rules.mjs`. See
[`docs/rules/brief.md`](rules/brief.md) for what CI enforces and what it cannot.

Decided on wayfinder ticket
[#12](https://github.com/elpideus/demido-studio/issues/12).

## How to read it

**Id.** Assigned once, from the next free number, and never reused or
renumbered, so a citation written today still resolves in a year. The table is
ordered by the **brief**, not by id: a requirement noticed later takes the next
free id and sits in its brief position, which is why `B49` appears between `B27`
and `B28`. A requirement that dies keeps its row and its id, struck through.

**Brief says.** A verbatim fragment of `brief.md`, long enough to find the line
and no longer. CI matches it against the brief character for character, so a row
cannot drift from the thing it claims to track.

**Decided.** The ticket that settled how this requirement is met, or `-`.

**Built.** Where the code lives, or `-`. Almost everything is `-`: the workspace
landed on [#38](https://github.com/elpideus/demido-studio/issues/38) and the
slices are what fill this column in.

**Live.** A link to the Definition-of-Done evidence
([`docs/rules/done.md`](rules/done.md): a live-model scenario and a screenshot of
the running window), or `-`. `n/a` where no model can exercise the requirement,
such as the licensing and attribution rows.

Three columns rather than one because they are three different failures, and v2
hit two of them. Milestones 10 and 11 were **decided but never checked against
the brief**. Twelve features were **built, unit-tested and never once driven**,
and the first live run found a bug in the newest of them within one question. A
single "covered" column hides exactly the distinction that set v2 aside.

## The ledger

| Id | Brief says | Decided | Built | Live |
|---|---|---|---|---|
| B01 | "check all the skills, plugins, and tools you have access to" | [#1](https://github.com/elpideus/demido-studio/issues/1) | - | n/a |
| B56 | "I need to create apps on developer dashboards, provide API keys, etc." | [#17](https://github.com/elpideus/demido-studio/issues/17) | - | - |
| B02 | "a harness that integrates all the tools I need seamlessly" | [#17](https://github.com/elpideus/demido-studio/issues/17) | - | - |
| B03 | "windows-only at first, during the pre-release versions" | [#31](https://github.com/elpideus/demido-studio/issues/31) | `src-tauri/tauri.conf.json`, `scripts/check-release.mjs`, `src-tauri/crates/demido-hardware/`: one NSIS installer at `currentUser` and no second target, checked at release time, over an accelerator probe gated on Windows and one line from the platform after it | [#49](https://github.com/elpideus/demido-studio/issues/49) |
| B04 | "get the most out of even smaller models" | [#13](https://github.com/elpideus/demido-studio/issues/13), [#22](https://github.com/elpideus/demido-studio/issues/22) | `src-tauri/crates/demido-prompts/`, the paragraph register, and the wordings the turn loop answers a call it did not run with: declined, repeated, switched off, stopped, past the step limit, and past the delegation depth ([#64](https://github.com/elpideus/demido-studio/issues/64)). `tools.repeated` is the first of them written from a measurement rather than from taste ([#59](https://github.com/elpideus/demido-studio/issues/59)): told the declined paragraph twice, the development model made the identical write three times in one turn, so a repeat is now told it is a repeat and told what to do instead, and the step after a repeat is sent with no tools at all | [#59](https://github.com/elpideus/demido-studio/issues/59), partly: the first evidence, and it is mixed. A model handed a refusal still repeats the call it was refused in two runs out of ten, and nothing here moves that number yet, [#69](https://github.com/elpideus/demido-studio/issues/69), partly: the parent's last prompt was 1196 tokens across a delegation against 3881 for the same task inline on the development tier, and no tier elects to delegate unprompted yet |
| B58 | "or the user tells it where it is wrong" | [#13](https://github.com/elpideus/demido-studio/issues/13), [#23](https://github.com/elpideus/demido-studio/issues/23) | - | - |
| B05 | "NOT this specific issue, but this specific KIND of issue" | [#13](https://github.com/elpideus/demido-studio/issues/13), [#22](https://github.com/elpideus/demido-studio/issues/22) | - | - |
| B06 | "All prompts should be editable. Requests monitorable." | [#8](https://github.com/elpideus/demido-studio/issues/8), [#33](https://github.com/elpideus/demido-studio/issues/33) | `src-tauri/crates/demido-prompts/`, both registers: the paragraphs, and one document per host tool, its description and parameter prose hashed together ([#52](https://github.com/elpideus/demido-studio/issues/52)). The log half of monitorable in `src-tauri/crates/demido-trace/`, where `tools/offered` records a name and a hash per tool. The window half is `web/src/monitor/` ([#57](https://github.com/elpideus/demido-studio/issues/57)): the session monitor opens pinned under the desk, groups the log into turns, colours and icons every row by its source from the eight `--src-*` tokens, rebuilds the assembly as it stood at any event block by block and diffed against the one before it, in the tool wording that was actually sent, says whether an absent tool group was switched off, dropped or withheld by the loop after a step that ran nothing, and ends in a tab that is the raw JSON of the line. Editable prompts are `web/src/settings/Prompts.tsx` ([#58](https://github.com/elpideus/demido-studio/issues/58)): a Settings page listing every paragraph-register entry by id, each opening into an editable field, nothing read-only, an entry's dependants above the field, an edit suppressing a measured claim rather than being refused, and a default that moved under an older edit carrying its note and a diff. The tool register's editor is `web/src/settings/ToolDocuments.tsx` ([#77](https://github.com/elpideus/demido-studio/issues/77)): a Settings page of its own, one field per tool holding its description and its parameter prose as one document with one hash, the schema's shape drawn above it and never editable, every entry declaring what it is load-bearing for, and a live run a document was driven against struck once it is edited | [#49](https://github.com/elpideus/demido-studio/issues/49), [#59](https://github.com/elpideus/demido-studio/issues/59) |
| B07 | "recorded in an append-only session log" | [#8](https://github.com/elpideus/demido-studio/issues/8), [#26](https://github.com/elpideus/demido-studio/issues/26) | `src-tauri/crates/demido-trace/`, and `web/src/monitor/` reading it ([#57](https://github.com/elpideus/demido-studio/issues/57)). A step that ran nothing records the tools being withheld as a set of its own under `Layer::Withheld`, so the rebuild of every step is the assembly that step was sent with ([#59](https://github.com/elpideus/demido-studio/issues/59)). Every event names the agent that produced it, the main session's included, so a sub-agent is a scope on one stream rather than a log of its own: `agent/delegated` carries the call that opened a child and the depth that is its indent, `agent/returned` is written only where a background answer is folded in, and a child writes its own `tools/offered` and its own wordings so its assembly rebuilds out of its own events ([#62](https://github.com/elpideus/demido-studio/issues/62)) | [#49](https://github.com/elpideus/demido-studio/issues/49), [#59](https://github.com/elpideus/demido-studio/issues/59), [#69](https://github.com/elpideus/demido-studio/issues/69): a sub-agent's assembly rebuilt from the log byte for byte against what `llama.cpp` received, and every block of the parent's assemblies its own |
| B08 | "reverting back the session and files to a previous status" | - | - | - |
| B09 | "both at a model/global level as well as at a per-chat level" | [#8](https://github.com/elpideus/demido-studio/issues/8), [#32](https://github.com/elpideus/demido-studio/issues/32) | `src-tauri/crates/demido-settings/`, `web/src/settings/`, two tiers live and four stored | [#44](https://github.com/elpideus/demido-studio/issues/44), [#49](https://github.com/elpideus/demido-studio/issues/49), [#69](https://github.com/elpideus/demido-studio/issues/69) |
| B61 | "since I will need a character system implemented later down the road so users can give LLMs personalities and ways of doing things" | [#32](https://github.com/elpideus/demido-studio/issues/32) | `src-tauri/crates/demido-settings/`: the character tier of the stored shape, and the system prompt as a ladder value rather than a field on a chat | - |
| B10 | "The codebase should be modular and simple by design." | [#10](https://github.com/elpideus/demido-studio/issues/10) | `src-tauri/` | [#49](https://github.com/elpideus/demido-studio/issues/49) |
| B11 | "Guided set-up on first launch" | [#21](https://github.com/elpideus/demido-studio/issues/21), [#27](https://github.com/elpideus/demido-studio/issues/27), [#28](https://github.com/elpideus/demido-studio/issues/28), [#29](https://github.com/elpideus/demido-studio/issues/29), [#30](https://github.com/elpideus/demido-studio/issues/30) | `src-tauri/crates/demido-hardware/`, `src-tauri/crates/demido-catalog/`, `src-tauri/crates/demido-runtimes/`, `src-tauri/crates/demido-setup/`, `web/src/setup/`: the accelerator detected, pre-selected and still a row, the manifest it picks from, the fetch that verifies what it took and owns it afterwards, and the wizard over the desk whose every step renders the control the settings page renders | [#48](https://github.com/elpideus/demido-studio/issues/48), partly: driven on a configured profile, not from zero ([#79](https://github.com/elpideus/demido-studio/issues/79)) |
| B12 | "Multi-account system" | [#15](https://github.com/elpideus/demido-studio/issues/15) | - | - |
| B13 | "Two main chat modes: Chat and Agent." | [#20](https://github.com/elpideus/demido-studio/issues/20) | - | - |
| B57 | "There should be 3 agent modes: Cautious, Balanced, Autonomous." | [#20](https://github.com/elpideus/demido-studio/issues/20), [#53](https://github.com/elpideus/demido-studio/issues/53) | `src-tauri/crates/demido-permission/`: the matrix, three rows and the destructive floor, read by the turn loop on every call ([#54](https://github.com/elpideus/demido-studio/issues/54)). The mode is `tools.mode` on the settings ladder, resolved per message with the chat last, and the composer's mode control sets it, Cautious by default with a shield in the shell (`web/src/shell/ModeControl.tsx`, [#56](https://github.com/elpideus/demido-studio/issues/56)). Cautious decays by the person's choice rather than by nagging: *always for this tool* on the approval row writes `tools.always` at the chat tier alone and never covers a destructive call ([#55](https://github.com/elpideus/demido-studio/issues/55)) | [#59](https://github.com/elpideus/demido-studio/issues/59), [#69](https://github.com/elpideus/demido-studio/issues/69): a child of a Cautious parent asked about its own write, and a delegation asked about once per turn |
| B14 | "Deep integration of Caveman-like system" | - | - | - |
| B15 | "Tool calling and custom tools" | [#14](https://github.com/elpideus/demido-studio/issues/14), [#20](https://github.com/elpideus/demido-studio/issues/20) | `src-tauri/crates/demido-tools/`: the trait, the registry, the Files group with every path confined to a workspace, and the Shell group, `run_command` ([#51](https://github.com/elpideus/demido-studio/issues/51)), whose process tree dies with the call. Each tool described in words from the tool register ([#52](https://github.com/elpideus/demido-studio/issues/52)). `src-tauri/crates/demido-chat/`: the turn loop ([#54](https://github.com/elpideus/demido-studio/issues/54)), which offers them in the payload, rules on each call through the matrix, asks through a callback, runs it, and sends the result back, under a step limit from the ladder; driven by a scripted backend that passes the `Backend` contract. What is offered is `tools.offered` on the settings ladder, set from the composer's tool picker (`web/src/shell/ToolPicker.tsx`, [#56](https://github.com/elpideus/demido-studio/issues/56)): a tool switched off is absent from the payload, and a call naming it is told the user turned it off. A call, its result and the one place a person decides are drawn in the transcript where the turn is (`web/src/chat/ToolCall.tsx`, `web/src/chat/Approval.tsx`, [#55](https://github.com/elpideus/demido-studio/issues/55)): a long result folds, a failed one reads as failed, and the approval is a row with the tool, its declared ability and the exact arguments, Enter and Escape at `panel` scope (`web/src/shell/keys.ts`). The window points at a folder with `DEMIDO_WORKSPACE` until projects do it. A step whose every call came back refused takes the tools away for the rest of the turn, recorded as a set of its own so the monitor says who withheld them ([#59](https://github.com/elpideus/demido-studio/issues/59)) | [#59](https://github.com/elpideus/demido-studio/issues/59) |
| B16 | "Markdown, LaTeX and Mermaid supported in the chat bubbles." | - | `web/src/chat/Markdown.tsx`, Markdown and fenced code only: LaTeX and Mermaid have no decision ticket and are not S1's to invent | - |
| B17 | "Projects system that allows to group chats" | [#8](https://github.com/elpideus/demido-studio/issues/8) | - | - |
| B18 | "Native web search and fetch" | - | - | - |
| B19 | "Configurable Agents & sub-agents system." | [#8](https://github.com/elpideus/demido-studio/issues/8), [#11](https://github.com/elpideus/demido-studio/issues/11), [#14](https://github.com/elpideus/demido-studio/issues/14), [#37](https://github.com/elpideus/demido-studio/issues/37) | `src-tauri/crates/demido-permission/src/inherit.rs`: what a sub-agent inherits, as one pure function over the parent's resolution and the child's request, so the offered set and the mode are ceilings at every depth and widening is unrepresentable rather than refused ([#60](https://github.com/elpideus/demido-studio/issues/60)). `src-tauri/crates/demido-tools/src/delegate.rs`: `delegate_task`, one registry entry in a Delegation group, declaring `Ability::Shell` and adding no ability to the matrix, described in a tool-register document from its first commit, and handing its task to a callback because a tool may know about no session, backend or ladder ([#61](https://github.com/elpideus/demido-studio/issues/61)). One prompt per turn rather than one per sub-agent is `demido_permission::answers_for_the_turn`, read by the turn loop into the turn's own copy of `always` and never written to the ladder. The log half is `demido-trace`'s agent field, written in the slice's first commit rather than retrofitted, with `Replay::agents` as the monitor's left column ([#62](https://github.com/elpideus/demido-studio/issues/62)). `src-tauri/crates/demido-chat/src/chat.rs`'s `Agent`: a delegated task is a **child session**, a second recorder over the conversation's own journal handle that runs the whole agent loop again in a separate clean context, with `inherit` called on the way into every child at every depth and the parent's cancellation token shared down the chain, so a Stop leaves nothing generating at depth 2 ([#63](https://github.com/elpideus/demido-studio/issues/63)). `src-tauri/crates/demido-chat/src/delegation.rs` is the two ends of a delegation, made together: the tool holds the sending end because a registry entry outlives every turn it is offered in, and the turn that asked carries the task out, so at parallelism 1 the call blocks and the answer is the tool's own result. The depth is one number on the ladder, `tools.delegation_depth`, defaulting to 2 and read where a delegation is dispatched: a resolution holds its level rather than a remaining count, and `delegate_task` is in a child's derived set while depth remains and **absent** when it does not, which is the same absence the picker produces. A child that names it anyway is told so in `tools.depth`'s own words rather than the picker's, as an event the monitor draws a row for, so it answers from what it has ([#64](https://github.com/elpideus/demido-studio/issues/64)). `src-tauri/crates/demido-vram/`: parallelism as a VRAM budget rather than a preference, because the brief's own clause is "by people whose systems can afford the parallelization". A sub-agent runs the conversation's own weights on a second `llama.cpp` slot, never a second model, so S1's single-model permit still holds and a slot is a KV reservation that `--ctx-size` multiplies. `admit` is a pure function over free bytes, the cost of a slot and the slots already open, so the rig's own figures are asserted on a machine with no card; `free_now` is the real NVML reading beside it, taken at the load rather than when a settings page was drawn. A slot that does not fit is queued with a stated reason, never opened and never evicting, and `Presence::Ready` carries the slots the backend reports rather than the ones the ladder asked for. `tools.parallel_agents` defaults to 1, the synchronous path and the only value the reference model can honour on the rig ([#65](https://github.com/elpideus/demido-studio/issues/65)). `src-tauri/crates/demido-chat/src/flight.rs`: the asynchronous path. Above the default the call is answered at once, because a request whose assistant message asks for a call nothing answered is one no compatible server accepts, and the sub-agent runs beside the turn that asked for it. What it said arrives at a **step boundary** as a framed message derived from `agent/returned`, never as a second result for a call that already has one, and the determinism is an ordering on the log rather than control of a clock: a background answer appears after every call of its step is answered and nowhere else, and two of them appear in the order their delegations were asked for. **A run may not end with a delegation in flight**: a model that stops asking for tools waits and folds it in, and a run past its step ceiling waits and writes it down anyway, because a delegation nobody mentions again is a silent loss and a ceiling that ate an answer would be worse than the runaway it ends. One rendezvous per agent, since two concurrent loops sharing one is a sub-agent carried out correctly and recorded under the wrong parent ([#66](https://github.com/elpideus/demido-studio/issues/66)). `web/src/chat/Delegation.tsx` is what a person sees of all of it: a delegation is **one exchange** in the transcript, the task out and the answer back, framed as a sub-agent's answer rather than as the model's own prose or a tool's output. Which row the transcript draws is the projection's rather than the window's, off the `agent/delegated` events rather than off a tool name, and the answer drawn is the child's own ending rather than the call's result, which on the background path is the paragraph saying the work has gone out. A delegation that opened no sub-agent stays an ordinary call row with its stated reason, and the floor holds through the indirection: *always* on the delegation leaves a destructive call inside the child asking ([#67](https://github.com/elpideus/demido-studio/issues/67)). `web/src/monitor/Scope.tsx` is the monitor's agent scope, which replaced the Sub-Agents window: the agents on top of the left column off `Replay::agents`, depth as the indent and delegated rows in `--color-violet`, `Main session` as the way back to the unscoped run, and selecting an agent filters the stream and the source ledger beneath it. The rebuild works inside a child because it resolves the agent of the event selected, so a sub-agent's assembly carries the offered set it was actually given. At the monitor's floor the column drops to icons ([#68](https://github.com/elpideus/demido-studio/issues/68)). `src-tauri/crates/demido-chat/tests/a_real_model_delegating.rs` is the slice's model gate: twelve scenarios, the returning one and the election on all three tiers, with `tests/replayed.rs` rebuilding the committed trace of a sub-agent's assembly byte for byte with no card ([#69](https://github.com/elpideus/demido-studio/issues/69)) | [#69](https://github.com/elpideus/demido-studio/issues/69), `Bar: chose` on the delegation returning and `used` on electing to: all three tiers delegated when told and answered from the child, and none of the three reached for `delegate_task` with nothing naming it |
| B63 | "Also user should be able to manually set the amount of parallel agents they want to run at the same time, and how deep agents can delegate one another" | [#8](https://github.com/elpideus/demido-studio/issues/8), [#11](https://github.com/elpideus/demido-studio/issues/11), [#37](https://github.com/elpideus/demido-studio/issues/37) | `tools.parallel_agents` and `tools.delegation_depth` on the settings ladder, global then chat ([#64](https://github.com/elpideus/demido-studio/issues/64), [#65](https://github.com/elpideus/demido-studio/issues/65)), drawn in the header of the monitor's agent column with the settings page's own control, following global until this chat overrides one. A ceiling the card set is drawn as the card's rather than as the person's number: `Presence::Ready` carries `limit`, the admission's stated reason, and the header says the card holds fewer beside the preference that asked for more. The slot strip reads filled, queued and free, filled counted off the log as agents with a request out (`Agent::generating`) ([#68](https://github.com/elpideus/demido-studio/issues/68)) | [#69](https://github.com/elpideus/demido-studio/issues/69): depth 1 leaves the child without `delegate_task` and depth 2 opens the chain; parallelism 3 ran two sub-agents at once on the development model, against a pool that prices a slot at #65's weighed figure because the build measures none yet ([#105](https://github.com/elpideus/demido-studio/issues/105)), and 2 on the reference model opened one slot with the reason stated as unmeasured |
| B20 | "Skills system. This is important because my requirements for it are complex." | [#14](https://github.com/elpideus/demido-studio/issues/14) | - | - |
| B50 | "I also need skills to be able to provide custom tools that LLMs can use" | [#14](https://github.com/elpideus/demido-studio/issues/14) | - | - |
| B51 | "The file that gets actually loaded (if skill is enabled) is the skill.json file" | [#14](https://github.com/elpideus/demido-studio/issues/14) | - | - |
| B59 | "There are also the slash commands, that allow users to use features from the skill on demand" | [#25](https://github.com/elpideus/demido-studio/issues/25) | - | - |
| B60 | "The "when" condition applies to these too." | [#14](https://github.com/elpideus/demido-studio/issues/14), [#25](https://github.com/elpideus/demido-studio/issues/25) | - | - |
| B52 | "skills also provide the required mcps and tools instead of the user having to grab them manually" | [#14](https://github.com/elpideus/demido-studio/issues/14) | - | - |
| B53 | "The ones without a specific type are assumed to be the ones provided by the skill in an "engine" or "src" folder" | [#14](https://github.com/elpideus/demido-studio/issues/14) | - | - |
| B54 | "The prompt type means that the tool is nothing more than an md file that gets read and tells LLM what to do on the spot." | [#14](https://github.com/elpideus/demido-studio/issues/14) | - | - |
| B62 | "the skill creator might need to change the tool description to better fit in their skill" | [#14](https://github.com/elpideus/demido-studio/issues/14), [#33](https://github.com/elpideus/demido-studio/issues/33) | - | - |
| B21 | "Artifact system." | [#7](https://github.com/elpideus/demido-studio/issues/7), [#10](https://github.com/elpideus/demido-studio/issues/10) | - | - |
| B22 | "Models Browser & Downloader" | [#7](https://github.com/elpideus/demido-studio/issues/7) | `src-tauri/crates/demido-models/src/index.rs`, `src-tauri/src/models.rs`: the index, keyless Hugging Face, parsed as pure functions over bytes against payloads committed in the repo; a listing capped at thirty and ordered by downloads, gating read at listing time, publisher fields passed through verbatim with no capability inferred, a `User-Agent` naming the app, and an unreadable index answered as a stated condition rather than an error ([#70](https://github.com/elpideus/demido-studio/issues/70)); `src-tauri/crates/demido-models/src/quant.rs`, `choices.rs`: a repository's files read into what a person can choose, each quantisation an ordered value carrying llama.cpp's spelling and published bits per weight, an unpublished label kept as written with no number and sorted last, a split model one choice whose pieces are its shards, a projector fetched with the weights that need it and never offered, a draft never offered, and the size the server's own figure ([#71](https://github.com/elpideus/demido-studio/issues/71)); `src-tauri/crates/demido-download/`, `src-tauri/src/downloads.rs`: the queue, per profile, with per-item pause, resume and cancel and `Pause all`, a failure a row with its cause that is retried in place from the bytes on disk while the other downloads carry on, the queue file recording intent and the partial files progress so a restart resumes, a cancel deleting its partial files, free space asked before the transfer, and every file checked against the listed size, its published digest and its GGUF header before the model is renamed into the library, tested against a local server that misbehaves on purpose and against Hugging Face itself ([#73](https://github.com/elpideus/demido-studio/issues/73)); `web/src/shell/TitleBar.tsx`, `web/src/downloads/`: the application title bar drawn by the window, keeping its minimise, and the download indicator in it (idle, active with the bytes still to arrive, failed with how many), opening the queue with per-item pause, resume and cancel and `Pause all`, progress in bytes against the total, a failed row stating its cause with retry in place, and a finished row offering its model at once and re-reading the library; progress held in a ref flushed once a frame rather than in state ([#76](https://github.com/elpideus/demido-studio/issues/76)); `src-tauri/crates/demido-vram/src/fit.rs`, `src-tauri/src/fit.rs`, `web/src/models/FitVerdict.tsx`: the fit verdict, stated before a download and never blocking one, the load priced as one slot through the pool's own `admit` so the two share a boundary, in the shape of `done.md`'s table and table-tested against its rows without a card, the card read each time the pane asks with what the resident model was weighed at holding given back, a context not priced before download said rather than counted as nothing, and drawn in ink; `demido-vram` promoted in the port ledger with both consumers named ([#74](https://github.com/elpideus/demido-studio/issues/74)); `web/src/models/Browser.tsx`: the Models window in two panes as a plain flex row with no container query, one component rendered by the composer's model control, the empty state's Browse models and the wizard's models step; search, the `All`, `Installed` and `GGUF` tags, a row per model with its capability icons, and a footer naming the folders read; the publisher's facts (params, architecture and context read by the Hub from the published GGUF's header, `index::Repo`), download options, and one primary action stating the size beside the fit verdict; capabilities on disk read from the file and greyed where absent, a repository's only what its publisher states in its pipeline or tags and `Unknown` otherwise, drawn apart from absent; gating said before a download, an empty result said plainly, and an unreadable index stated above the installed models ([#75](https://github.com/elpideus/demido-studio/issues/75)) | [#75](https://github.com/elpideus/demido-studio/issues/75), window gate only: driven over CDP through both hosts, a gated repository, an empty search, a dead connection and a download queued from the pane |
| B55 | "Multiple folders should be set-able for model detection" | [#15](https://github.com/elpideus/demido-studio/issues/15), [#48](https://github.com/elpideus/demido-studio/issues/48) | `src-tauri/crates/demido-models/`, `src-tauri/src/models.rs`, `web/src/setup/Controls.tsx`: the download folder and the scan folders as two per-profile settings on the ladder, the scan folders seeded by what LM Studio and the others already fill, and the library read from them with no bookkeeping file: borrowed folders read and never written, removal refused outside Demido's own folder, a borrowed row naming its library, borrowed bytes left out of the disk Demido spent, and every model's header verified and its capabilities read from the file before it is offered ([#72](https://github.com/elpideus/demido-studio/issues/72)) | [#48](https://github.com/elpideus/demido-studio/issues/48), partly: driven on a configured profile, not from zero ([#79](https://github.com/elpideus/demido-studio/issues/79)) |
| B23 | "LLM capabilities support. Vision, Thinking toggling" | - | - | - |
| B24 | "Task-model functionality." | [#24](https://github.com/elpideus/demido-studio/issues/24) | - | - |
| B25 | "Accounts management system" | [#5](https://github.com/elpideus/demido-studio/issues/5), [#17](https://github.com/elpideus/demido-studio/issues/17), [#26](https://github.com/elpideus/demido-studio/issues/26) | - | - |
| B26 | "Google Account OAuth" | [#3](https://github.com/elpideus/demido-studio/issues/3), [#5](https://github.com/elpideus/demido-studio/issues/5), [#17](https://github.com/elpideus/demido-studio/issues/17), [#26](https://github.com/elpideus/demido-studio/issues/26) | - | - |
| B27 | "Market Data accounts" | - | - | - |
| B49 | "Any other accounts added in the future." | [#26](https://github.com/elpideus/demido-studio/issues/26) | - | - |
| B28 | "I want the system to be called Nexus." | [#4](https://github.com/elpideus/demido-studio/issues/4), [#18](https://github.com/elpideus/demido-studio/issues/18), [#26](https://github.com/elpideus/demido-studio/issues/26) | - | - |
| B29 | "Native graphify or graphify-like implementation" | [#8](https://github.com/elpideus/demido-studio/issues/8) | - | - |
| B30 | "Email, Calendar and Contacts implementation" | [#3](https://github.com/elpideus/demido-studio/issues/3), [#17](https://github.com/elpideus/demido-studio/issues/17), [#26](https://github.com/elpideus/demido-studio/issues/26) | - | - |
| B31 | "Charts visualizer" | [#8](https://github.com/elpideus/demido-studio/issues/8) | - | - |
| B32 | "Chat export system." | [#8](https://github.com/elpideus/demido-studio/issues/8) | - | - |
| B33 | "Full Keyboard navigation" | [#8](https://github.com/elpideus/demido-studio/issues/8) | - | - |
| B34 | "Mac Spotlight-like command panel." | [#8](https://github.com/elpideus/demido-studio/issues/8) | - | - |
| B35 | "Integrated basic Web Browser" | [#10](https://github.com/elpideus/demido-studio/issues/10), [#28](https://github.com/elpideus/demido-studio/issues/28) | - | - |
| B36 | "it should be simple, clean, dark and minimal" | [#7](https://github.com/elpideus/demido-studio/issues/7), [#9](https://github.com/elpideus/demido-studio/issues/9) | - | - |
| B37 | "on par with APPLE Design Team" | [#9](https://github.com/elpideus/demido-studio/issues/9) | - | - |
| B38 | "Use a pastel-colors palette for a more polished feel." | [#6](https://github.com/elpideus/demido-studio/issues/6) | - | - |
| B39 | "keep in mind to not mix-match systems for no reason" | [#10](https://github.com/elpideus/demido-studio/issues/10) | - | - |
| B40 | "Keep arbitrary values like colors localized" | [#9](https://github.com/elpideus/demido-studio/issues/9) | - | - |
| B41 | "There should be a splash small window" | [#6](https://github.com/elpideus/demido-studio/issues/6) | `web/src/splash/`, `src-tauri/src/boot.rs` | [#47](https://github.com/elpideus/demido-studio/issues/47) |
| B42 | "a VSCode-like Icons-only sidebar" | [#7](https://github.com/elpideus/demido-studio/issues/7), [#8](https://github.com/elpideus/demido-studio/issues/8) | `web/src/shell/`, `src-tauri/crates/demido-shell/` | [#42](https://github.com/elpideus/demido-studio/issues/42), [#49](https://github.com/elpideus/demido-studio/issues/49) |
| B43 | "Some panels should be movable by using a modifier key" | [#7](https://github.com/elpideus/demido-studio/issues/7), [#10](https://github.com/elpideus/demido-studio/issues/10) | - | - |
| B44 | "you should test the project yourself" | [#11](https://github.com/elpideus/demido-studio/issues/11) | - | n/a |
| B45 | "credited both in the source code and inside the program itself" | [#16](https://github.com/elpideus/demido-studio/issues/16), [#27](https://github.com/elpideus/demido-studio/issues/27) | - | n/a |
| B46 | "A licenses folder will also be needed" | [#16](https://github.com/elpideus/demido-studio/issues/16), [#27](https://github.com/elpideus/demido-studio/issues/27), [#28](https://github.com/elpideus/demido-studio/issues/28) | [`licenses/`](../licenses/README.md) | n/a |
| B47 | "should not be listed as a Contributor/Co-Author in git" | - | - | n/a |
| B48 | "Repo name should be" | [#1](https://github.com/elpideus/demido-studio/issues/1) | - | n/a |

## Amendments

The brief wins until Stefan says otherwise in writing. Eight lines have been
overruled so far, each on a ticket, each with the reasoning on that ticket.
`brief.md` itself is never edited: its verbatim guarantee is the whole value of
the file. This section is how a session reading the brief learns which lines no
longer bind, and CI checks that each original below still matches `brief.md`
character for character.

| Id | The brief said | Overruled by | What binds instead |
|---|---|---|---|
| B38 | "Use a pastel-colors palette for a more polished feel." | [#6](https://github.com/elpideus/demido-studio/issues/6) | **Karl**, Fluent 2 dark surfaces carrying v2's accents. Pastel lost on chroma, not contrast: 0.066 of separation from neutral ink against the saturated green's 0.182, and sRGB refuses more than about 0.08 at pastel lightness. |
| B42 | "Sub-Agents monitoring window" | [#8](https://github.com/elpideus/demido-studio/issues/8) | Sub-agents are a scope on the session log, not a window, so the rail loses that entry. |
| B43 | "The panels system should be inspired by Hyprland and i3 tiling systems on linux." | [#7](https://github.com/elpideus/demido-studio/issues/7) | Windows 11 and KDE behaviour: floating covers, pinned makes the desk give up exactly its width, no pin button and no minimise. |
| B35 | "Integrated basic Web Browser that both users and LLMs have access to" | [#10](https://github.com/elpideus/demido-studio/issues/10) | One panel over two engines: the user on WebView2, the model on Chromium over CDP, because WebView2 has no isolated worlds. |
| B12 | "allowing different users to use the software on the same machine" | [#15](https://github.com/elpideus/demido-studio/issues/15) | A Demido profile is a Windows profile. The separation is the operating system's, not Demido's: a second person makes a second Windows user. Two people at one Windows login share one profile entirely, and Demido neither detects nor warns about that. |
| B02 | "a harness that integrates all the tools I need seamlessly" | [#17](https://github.com/elpideus/demido-studio/issues/17) | Two audiences, not one promise: no setup on the common path, and every credential visible and replaceable underneath. Google mail is one click through an app password, but Google calendar and contacts cost a guided ten minutes in the Cloud console, because Demido ships no OAuth client of its own and verification needs a domain it does not have. |
| B13 | "Two main chat modes: Chat and Agent." | [#20](https://github.com/elpideus/demido-studio/issues/20) | One loop, always the agent loop, the shape Claude Code has. A conversation that calls no tool is the same loop with nothing to do but answer. Everything the Chat mode would have done is now a set with nothing in it on the offered axis, reached by a control that can also say "files but no shell", which no mode could express. |
| B60 | "The "when" condition applies to these too." | [#14](https://github.com/elpideus/demido-studio/issues/14) | Nothing evaluates `when` on a slash command, because the user chose it. It is help text on the command and is documented as help text. The clause still binds the skill itself: `when` at the top level says when to read the skill, and `agent.when` says when to delegate to it. |
