# Evidence

Screenshots the window gate owes (`docs/rules/done.md`), one set per ticket, kept off `main` on purpose: `/evidence` is ignored in a clone, and this branch exists only so the images have a URL an issue can render.
