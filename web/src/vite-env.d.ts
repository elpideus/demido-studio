/// <reference types="vite/client" />

/** Injected by `vite.config.ts` from `src-tauri/tauri.conf.json`, so the window
 * and the installer cannot disagree about which build this is. */
declare const __APP_VERSION__: string
